function [ xLand ] = LandingDistance( d,v0,theta )
%Average normal and drag landing distances

empiricalData = xlsread('EmpiricalData.xlsx');
angles = empiricalData(:,1);
distance = empiricalData(:,2);

empiricalFunction = polyfit(angles,distance,7);

x

end
